/**
 * @enum UserType is an enumeraton of the different user types that exist on our platform
 */
enum UserType {
  USER = "USER",
  COMPANY = "COMPANY",
  MODULE = "MODULE",
}

export default UserType;
